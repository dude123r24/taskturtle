# Tailwind CSS v4 @tailwindcss/oxide requires Node >= 20 and platform-specific
# native bindings. Use npm install (not npm ci) to ensure optional deps for the
# build platform are resolved correctly (npm/cli#4828).
[phases.install]
cmds = ["npm install --include=optional --no-audit --no-fund"]
